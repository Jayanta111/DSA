//Insert element in Doubly linked list
#include<iostram>
using namespace std;
struct node {
    struct node* prev;
    int data;
    struct node*next;
    
};
// Add-At-End insert Element in Linked List
struct node* addToempty(struct node* head,int data){
struct node* temp=malloc (sizeof(struct node));
temp->prev=NULL;
temp
}